import { MetersToFeetPipe } from './meters-to-feet.pipe';

describe('MetersToFeetPipe', () => {
  it('create an instance', () => {
    const pipe = new MetersToFeetPipe();
    expect(pipe).toBeTruthy();
  });
});
